# Profiling tools

Mesh scaling benchmark: nodes vs solve time.

1. Run `npm run benchmark` (from project root)
2. Start dev server: `npm run dev`
3. Open http://localhost:5173/tools/profiling/mesh-scaling-benchmark.html

Data is saved to `output/mesh-scaling.json`.
